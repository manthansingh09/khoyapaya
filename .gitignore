# Python
__pycache__/
*.pyc

# app/web/data.json and app/cases_full.json are generated by app/build_data.py
# but COMMITTED so Vercel can serve them (output is deterministic — no churn).
Volunteers_Synthetic.csv

# Secrets — never commit real values (use .env.example as the template)
.env
.env.local
.env.*.local

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
